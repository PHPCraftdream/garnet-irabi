{# No-access heading injected as page content body when an authenticated
   user hits a role-guarded route they cannot access. Plain <h1> by design
   — the surrounding HtmlLayout supplies all chrome. #}
<h1>{{ heading }}</h1>
